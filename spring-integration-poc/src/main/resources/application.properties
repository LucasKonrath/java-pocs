# Spring Integration POC Configuration
server.port=8080
spring.application.name=spring-integration-poc

# Logging configuration
logging.level.org.springframework.integration=DEBUG
logging.level.org.example.springintegrationpoc=INFO

# Integration configuration
spring.integration.poller.default-timeout=5000
